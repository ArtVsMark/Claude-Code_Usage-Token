{
  "_": "Правила, родившиеся ЗДЕСЬ и предлагаемые общему каталогу. Адрес этого файла называется в реестре каталога, .rules/consumers.json, поле proposals. Контракт: export/README.md каталога.",
  "_номер": "Номера здесь нет и быть не может: его присваивает каталог при приёме. Номера не переиспользуются, и если два проекта выберут номер независимо, столкновение уже нечем починить. Поля id, number, rule — ошибка, и прогон каталога о ней скажет.",
  "_что_слать": "Инцидент, а не готовую запись: что сломалось, с числами и последовательностью событий. Оба языковых дерева, словарь областей, ответ о соседях и разрешимый след живут в каталоге.",
  "_пусто": "Пустой список — законное состояние и означает «предлагать пока нечего». Отсутствие файла означает другое: «канал не подключён».",
  "schema": "1.0",
  "proposals": [
    {
      "slug": "git-file-list-needs-nul",
      "claim": "Перечисляя пути из git для последующего чтения, всегда передавай -z и разбирай вывод по NUL. Без него git экранирует имена с не-ASCII символами и пробелами, построенный путь не разрешается, и файл молча выпадает из обработки. Проверка, читающая такой список, обязана считать пропущенное и печатать охват: без числа просмотренного её слепота неотличима от чистого результата.",
      "incident": "Команда preflight запускает четыре гейта и проверяет файлы будущего коммита на секреты. Список файлов брался как `git ls-files --cached --others --exclude-standard` с разбором по строкам. Проверка на подделанных данных: в корень положен файл с заведомым токеном GitHub (ghp_ и 36 символов). Прогон вернул код 0 и напечатал «всё чисто: проверок 5». Токен не найден. Причина: файл назывался `утечка.py`, а git при core.quotePath=true (значение по умолчанию) отдаёт не-ASCII имя экранированным — \"\\321\\203\\321\\202...\". Код строил Path из экранированной строки, такого файла на диске нет, чтение падало с OSError, и обработчик `except (OSError, UnicodeDecodeError): continue` — написанный для двоичных файлов — глотал это молча. Из 29 файлов проверялось 28: ровно один, с кириллическим именем, был невидим. Внутренний язык проекта русский, то есть слепая зона приходилась на самый вероятный случай, а не на экзотику. До этого пробовали проверить гейт файлом с ASCII-именем — он находился, и проверка выглядела рабочей.",
      "trail": "ArtVsMark/claude-code-usage — scripts/preflight.py, функция tracked_files"
    },
    {
      "slug": "check-the-link-not-the-path",
      "claim": "Проверяя, что документ ССЫЛАЕТСЯ на файл, ищи ссылку, а не строку адреса. Адрес встречается в документе дважды — в цели ссылки и в её подписи, — и проверка на подстроку остаётся зелёной, когда цель подменили: условие выполняется на подписи. Гейт при этом держит ровно тот случай, который не ломается, и пропускает тот, ради которого заведён. То же относится ко всякой проверке отношения через присутствие подстроки: подстрока встречается и там, где отношения нет.",
      "incident": "Гейт витрины требует, чтобы обе витрины (README.md и README.en.md) вели к набору вопросов .rules/showcase.json: набор называет девять пробелов из десяти вопросов с причинами, и пробел, до которого с витрины не дойти, назван только для того, кто и так знает, где смотреть. Проверка была написана как `SHOWCASE_SET not in текст`. На подделанном дереве она краснела: тест убирал строку целиком, и находок было две — по одной на витрину. 62 теста при этом были зелёными. Проверка на провал на копии живого репозитория: у ссылки подменили адрес — `](.rules/showcase.json)` на `](nowhere)`, — а подпись ссылки оставили как есть: ``[`.rules/showcase.json`]``. Гейт остался зелёным: 10 вопросов, 1 живой значок, 0 находок. Путь никуда не делся, он остался в подписи, и условие выполнялось на ней. То есть проверка отсутствия ссылки не срабатывала ровно в том виде поломки, который случается: адрес меняют при переносе файла, а подпись остаётся прежней. Исправлено поиском самой ссылки (`](адрес` либо `]: адрес`); заведены обе регрессии — на упоминание без ссылки и на законную ссылку сноской, чтобы починка одной стороны не завернула другую.",
      "trail": "ArtVsMark/claude-code-usage — scripts/preflight.py, функция check_showcase и константа _НАБОР_ССЫЛКОЙ; задача #27"
    },
    {
      "slug": "shell-identifiers-must-be-ascii",
      "claim": "Внутренний язык проекта не распространяется на shell: в bash имя переменной обязано состоять из ASCII. Ошибка проявляется двумя способами, и второй опаснее. Присваивание с не-ASCII именем разбирается как ИМЯ КОМАНДЫ — прогон падает с кодом 127, и это заметно. Но переменная окружения с не-ASCII именем создаётся штатно, а `$ИМЯ` в bash не раскрывается вовсе: парсер требует ASCII-идентификатор, подстановка выходит пустой, и условие, написанное на такой переменной, всегда даёт одну и ту же ветку. Гейт при этом остаётся ЗЕЛЁНЫМ и не проверяет ничего. Правило: проза и идентификаторы языка, который их принимает, — на языке проекта; имена переменных shell — латиницей всегда.",
      "incident": "Проект ведётся по-русски: docstring'и, комментарии, тексты ошибок и даже идентификаторы Python — кириллицей, это записано в CLAUDE.md. Заводился гейт «запись changelog едет вместе с изменением» — workflow из трёх шагов. Первый шаг собирал список изменённых файлов: `файлы=$(git diff --name-only \"$BASE...$HEAD\" | tr '\\n' ' ')`. Прогон упал сразу: bash разобрал строку не как присваивание, а как запуск команды с именем `файлы=.github/workflows/changelog.yml`, вывод — `No such file or directory`, код возврата 127. Это обнаружилось прогоном и стоило одного цикла. Второй случай в том же файле не обнаружился бы вовсе: шаг «нужна ли запись» получал метки PR через `env:` с ключами `ТИП`, `ЧЕРНОВИК`, `ФОРК` и разбирал их как `case \" $ТИП \" in *\" bug \"*)`. Переменные окружения с такими именами создаются, но `$ТИП` в bash не раскрывается — подстановка пустая, ни один шаблон не совпадает, `required` остаётся `no`, и требование записи молча выключается. Гейт при этом зелёный: он отработал, ничего не проверив. То есть одна и та же ошибка дала громкий отказ в одном шаге и тихое «прошло» в соседнем.",
      "trail": "ArtVsMark/claude-code-usage — .github/workflows/changelog.yml, шаги «собрать список изменённых файлов» и «запись обязательна?»; задача #10, прогон 33374665692"
    },
    {
      "slug": "platform-readers-need-one-live-run",
      "claim": "Код, читающий состояние площадки (CI, pull request, очередь), тестами на подделанных снимках проверяется только со стороны своих правил чтения. Сами снимки при этом сочиняет автор, и вместе с ними он сочиняет свои допущения о том, что площадка присылает на самом деле. Ошибка в допущении даёт зелёный набор тестов и неработающий механизм. Поэтому каждый такой читатель обязан быть один раз прогнан вживую, а настоящие значения полей — записаны в тест фикстурой. До первого живого прогона механизм считается непроверенным, сколько бы тестов ни было зелёными.",
      "incident": "За одну серию из четырёх PR очередь мержей получила три дефекта подряд, и ни один не был виден по прогону тестов. (1) Вердикт «ветка отстала» читался из `mergeable_state == \"behind\"`. Тесты на снимках с таким значением проходили. Вживую площадка выставляет `behind` ТОЛЬКО при включённой защите ветки с требованием актуальности: PR #32 с базой 70bd5be при `main` на d4ec6a7 отдавал `mergeable_state: clean`, а `compare/main...head` — `behind_by: 1`. Гейт «зелено на моей ветке ≠ зелено после мержа» имел всегда зелёный вход. (2) Эталонный набор проверок брался как check-runs коммита `main`. Туда попадал check-run самой очереди («подвинуть очередь»), которого на pull request не создаётся никогда, — значит любому PR вечно недоставало одного имени. За первые полчаса конвейер не смержил ни одного PR из трёх зелёных. (3) Обход очереди просыпался по `workflow_run` с фильтром `branches: [main]`, но собственное действие очереди — обновление головы из `main` — запускает прогон на ветке PR, а не на `main`. Очередь засыпала до расписания сразу после того, как сама всё подготовила: за первый час два обхода и ноль мержей. Во всех трёх случаях набор тестов был зелёным и покрывал ровно то, что автор считал поведением площадки.",
      "trail": "ArtVsMark/claude-code-usage — scripts/merge_queue.py (функции main_state, behind_by), .github/workflows/merge-queue.yml; PR #35 и #36"
    },
    {
      "slug": "one-aggregating-required-check",
      "claim": "Защита ветки требует проверок ПО ИМЕНИ, и имя попадает в настройку дословно, вне репозитория и вне ревью. Отсюда два молчаливых отказа. Первый: имя, которого никто не создаёт, переводит PR в вечное ожидание — так ведёт себя и опечатка в настройке, и переименование джоба матрицы. Второй: ПРОПУЩЕННЫЙ джоб защита засчитывает как пройденный, поэтому агрегатор, собранный через `needs:`, при падении зависимости не краснеет, а пропускается — он разрешает мерж ровно тогда, когда обязан запретить. Правило: обязательная проверка ровно одна, имя её постоянно, она не зависит от других джобов через `needs:` и всегда доходит до вердикта, а совпадение её имени с настройкой держится тестом.",
      "incident": "Владелец включил на `main` ruleset с пустым списком обходов и одним обязательным контекстом `PR check` (2026-08-31T09:51:48Z, strict-политика). Проверки с таким именем в репозитории не было: джобы назывались `гейты · ubuntu-latest · python 3.13` (девять ячеек матрицы), «зона, тип и связь с задачей», «запись есть и она на языке проекта». Оба открытых PR (#33 и #37) имели по одиннадцать зелёных проверок по уникальным именам и `mergeable: true`, но `mergeable_state: blocked` — конвейер перестал мержить что-либо вовсе, и это состояние не покраснело нигде: сами проверки зелёные, очередь исправно писала «готов». Задача #6 назвала ловушку заранее — «имена джобов матрицы входят в список обязательных дословно; переименование ломает связь: PR будет вечно ждать проверку, которой больше нет», — и всё равно первым же включением защиты в неё попали, потому что список из одиннадцати имён нигде не сверяется с деревом. Починка: один агрегирующий джоб с постоянным именем, который читает check-runs головы PR и зеленеет только когда зелены все остальные; через `needs:` его собрать было нельзя ещё и потому, что гейты живут в трёх разных workflow намеренно — матрица не должна перезапускаться от навешивания метки, а гейты разметки обязаны.",
      "trail": "ArtVsMark/claude-code-usage — scripts/pr_check.py, .github/workflows/pr-check.yml; задача #6"
    }
  ]
}
