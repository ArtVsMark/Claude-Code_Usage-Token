Эталонный набор проверок берётся из джобов прогона `ci`, а не из check-runs
коммита общей ветки: туда попадал джоб самой очереди, которого на pull request
не бывает, и ни один PR не мог стать готовым.
