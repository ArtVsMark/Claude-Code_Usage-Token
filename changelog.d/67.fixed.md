Все скрипты ставят UTF-8 на свои потоки первым делом в `main`, и это держит гейт: раньше семь из одиннадцати печатали отказы в кодировке локали, а на windows-раннере такой отказ выходит как `\uXXXX`.
