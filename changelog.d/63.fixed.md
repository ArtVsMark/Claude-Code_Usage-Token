Гейт: текстовый режим подпроцесса обязан называть кодировку — без неё берётся локаль, и на windows-раннере русский вывод git роняет чтение. Починены два вызова в переписи ссылок; заодно гейты ставят UTF-8 на свои потоки, иначе их сообщения об отказе выходят как `\uXXXX`.
