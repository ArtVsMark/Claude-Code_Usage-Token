У каждого вызова подпроцесса появился дедлайн: раньше зависшую команду останавливал только таймаут джоба — минуты вместо секунд, — а локально не останавливало ничто. Заодно выключен терминальный запрос git: без него он при истёкшем ключе ждал ввода, которого в прогоне нет.
