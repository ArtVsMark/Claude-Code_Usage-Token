Гейт-перепись ссылок: адрес репозитория в дереве обязан называть нынешнее имя.
Переименование на GitHub оставляет редирект, отключить его нельзя, и старая
ссылка продолжает работать — значит не краснеет ничто. Замер: после трёх
переименований разом 28 ссылок устарели, а все восемь гейтов и прогон связи с
каталогом остались зелёными. Теперь имя вносится одной строкой в список
`АДРЕСА`, а остальное показывается пофайлово.
