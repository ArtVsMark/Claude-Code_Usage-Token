Очередь мержей просыпается от завершения **любой** проверки, которая ходит по
pull request, а не только прогона `ci`. Последним зеленеет `PR check` — он ждёт
всех остальных по построению, — и пока в списке стояло одно имя, момент
готовности PR очередь не видела никогда: готовый PR простоял 50 минут при нуле
обходов.
