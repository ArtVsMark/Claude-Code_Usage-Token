Подпись участника в истории общей ветки перестала двоиться: очередь собирает тело squash сама и снимает из него строку соавторства, которую пишет окно, — площадкина остаётся единственной. Версия модели живёт в ссылке `Claude-Session`, которую слияние не трогает.
