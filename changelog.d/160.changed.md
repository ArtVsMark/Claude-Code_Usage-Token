Значки витрины переехали на отдельную ветку `badges` — как у каталога и соседних проектов. В общей ветке их больше нет: значок там пересобирается от каждого изменения и даёт конфликт на каждом слиянии. Свежесть держит прогон на каждый толчок в `main`.
