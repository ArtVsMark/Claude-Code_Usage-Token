CLI пакета ставит UTF-8 на свои потоки: без этого на Windows отказ выходил как `\uXXXX` — и читал его человек, поставивший пакет. Гейт расширен на точки входа из `[project.scripts]`, у которых блока `__main__` нет.
