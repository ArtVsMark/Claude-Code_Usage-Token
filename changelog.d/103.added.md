Покрытие отвечает живым числом: прогон значков меряет его на одной названной ячейке и кладёт `coverage.json` на ветку `badges`, а поле `coverage_percent` появляется в фактах о проекте. Считаются обе половины дерева — сам инструмент и гейты конвейера.
