Прогон входящих называет каталог его нынешним именем в обоих вызовах — и в шаге входящих, и в обратных ссылках: умолчание в закреплённой версии осталось старым, и задача повела на имя, которого больше нет, а прогон при этом оставался зелёным — редирект работает.
